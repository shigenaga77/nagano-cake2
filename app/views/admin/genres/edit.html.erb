<h5 class="mx-auto col-xl-10 mt-5 ml-5">ジャンル編集</h5>
<%= form_with model: @genre, url: admin_genre_path do |f| %>
                
    <div class="mx-auto col-xl-11 mt-5">ジャンル名<span class="ml-5 mr-5"><%= f.text_field :name %></span><%= f.submit '変更を保存', class: 'btn btn-success' %></div>
<% end %>